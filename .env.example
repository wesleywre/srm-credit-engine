# PostgreSQL (docker compose)
POSTGRES_USER=srm
POSTGRES_PASSWORD=srm
POSTGRES_DB=srm_credit

# Portas expostas no host (altere se já estiverem em uso)
POSTGRES_PORT=5432
BACKEND_PORT=8000

# Backend
SRM_ENVIRONMENT=local
SRM_LOG_LEVEL=INFO
